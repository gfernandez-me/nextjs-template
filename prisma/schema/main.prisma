generator client {
  provider = "prisma-client-js"
  output   = "../generated/client"
  seed     = "npx tsx prisma/seed.ts"
}

datasource db {
  provider = "postgresql"
  url      = env("DATABASE_URL")
}
